import { Routes } from '@angular/router'

import { ProcessesListComponent } from './processes-list/processes-list.component'

const routes: Routes = [
  { path: 'processes-list', component: ProcessesListComponent }
]

export const processesListRoutes = routes
