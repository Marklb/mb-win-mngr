import { Routes } from '@angular/router'
import { VirtualDesktopUiComponent } from './virtual-desktop-ui.component'

const routes: Routes = [
  { path: 'virtual-desktop-ui', component: VirtualDesktopUiComponent }
]

export const virtualDesktopUiRoutes = routes
